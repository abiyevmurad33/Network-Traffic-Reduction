# Combat proxy scenario (v1): identical engine settings, different behavior via controller profile.

doom_game_path = ../freedoom2.wad
doom_scenario_path = basic.wad

episode_timeout = 2100
episode_start_time = 10
living_reward = 0
death_penalty = 0

window_visible = true
screen_resolution = RES_160X120
screen_format = CRCGCB

mode = PLAYER
automap_buffer_enabled = false
depth_buffer_enabled = false
labels_buffer_enabled = false
render_hud = false
render_crosshair = false
render_weapon = false
render_decals = false
render_particles = false
render_messages = false
render_corpses = false

# Must match canonical ACTION_LABELS order exactly:
# ['MOVE_FORWARD','MOVE_BACKWARD','MOVE_LEFT','MOVE_RIGHT','TURN_LEFT','TURN_RIGHT','ATTACK']
available_buttons = { MOVE_FORWARD MOVE_BACKWARD MOVE_LEFT MOVE_RIGHT TURN_LEFT TURN_RIGHT ATTACK }
